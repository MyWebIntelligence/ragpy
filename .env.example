# ============================================================
# RAGpy - Environment Variables Template
# ============================================================
# Copy this file to .env and fill in your API keys
# All variables are optional except OPENAI_API_KEY

# ------------------------------------------------------------
# OpenAI (REQUIRED for embeddings)
# ------------------------------------------------------------
OPENAI_API_KEY=sk-proj-...

# ------------------------------------------------------------
# OpenRouter (OPTIONAL - for cost-effective LLM text recoding)
# ------------------------------------------------------------
# If configured, you can use OpenRouter models (e.g., Gemini 2.5 Flash)
# for text recoding instead of OpenAI GPT-4o-mini, reducing costs by 2-3x.
# Format: provider/model (e.g., "openai/gemini-2.5-flash", "anthropic/claude-3-5-sonnet")
# Get your API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=
OPENROUTER_DEFAULT_MODEL=openai/gemini-2.5-flash

# ------------------------------------------------------------
# Pinecone (OPTIONAL - vector database)
# ------------------------------------------------------------
PINECONE_API_KEY=pcsk_...
PINECONE_ENV=https://your-index-name-xxxxx.svc.aped-xxxx-xxxx.pinecone.io

# ------------------------------------------------------------
# Weaviate (OPTIONAL - vector database)
# ------------------------------------------------------------
WEAVIATE_API_KEY=...
WEAVIATE_URL=https://xxxxx.c0.europe-west3.gcp.weaviate.cloud

# ------------------------------------------------------------
# Qdrant (OPTIONAL - vector database)
# ------------------------------------------------------------
QDRANT_API_KEY=...
QDRANT_URL=https://xxxxx.eu-central-1-0.aws.cloud.qdrant.io

# ============================================================
# Notes:
# - OPENAI_API_KEY is mandatory (used for embeddings generation)
# - OpenRouter credentials are optional (fallback to OpenAI if not set)
# - At least ONE vector database (Pinecone/Weaviate/Qdrant) should be configured
# - You can configure credentials via the web UI (Settings button)
# ============================================================
